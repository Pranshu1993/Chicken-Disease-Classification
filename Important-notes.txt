# @staticmethod
def save_model(path: str, model: tf.keras.Model):
    model.save(path)

But then you called it like an instance method:
self.save_model(path=..., model=...)


fixes :

1. remove @staticmethod
2. correct the call of the static method

PrepareBaseModel.save_model(path=self.config.base_model_Path, model=self.model)

Incorrect - self.save_model(path=self.config.base_model_Path, model=self.model)